[general]
accounts = Gmail
maxsyncaccounts = 3
ui = Blinkenlights
status_backend = sqlite

[Account Gmail]
localrepository  = Local
remoterepository = Remote

[Repository Local]
type = Maildir
localfolders = ~/Maildir

[Repository Remote]
type = IMAP
remotehost = imap.gmail.com
remoteuser = dustin.neuman@gmail.com
remoteport = 993
#remotepass = 'trixie is a little bixie!'
ssl        = yes
sslcacertfile = /etc/openssl/cert.pem
maxconnections = 1
realdelete = no
socktimeout = 90
autorefresh = 30


#[general] 
#accounts = Gmail, Dmail
#maxsyncconnections = 2
#ui = Blinkenlights
#socktimeout = 90

#[Account Gmail]
#localrepository = gmail-local
#remoterepository = gmail-remote
#autorefresh = 2

#[Account Dmail]
#localrepository = dmail-local
#remoterepository = dmail-remote
#autorefresh = 2 

#[Repository gmail-local]
#type = Maildir
#localfolders = ~/Mail/inbox

#[Repostiroy dmail-local]
#type = Maildir
#localfolders = ~/dMail/inbox

#[Repository gmail-remote]
#type = IMAP 
#remoteport = 993
#maxconnections=3
#remoteuser = dustin.neuman@gmail.com
#remotepass = 'trixie is a little bixie!'
#remotehost = imap.gmail.com
#ssl=true
#sslcacertfile = /etc/openssl/cert.pem
#status_backend=sqlite
#realdelete=no

#folderfilter = lambda foldername: foldername in ['[Google Mail]/INBOX']
#nametrans = lambda foldername: re.sub('^\[Google Mail\]/INBOX$', 'inbox', foldername)

#[Repository dmail-remote]
#type = IMAP
#remoteport = 993
#maxconnections = 3
#remoteuser = dustin@dustinneuman.com
#remotepass = 'trixiedixit666'
#remotehost = mail.dustinneuman.com
#ssl = true
#sslcacertfile = /etc/openssl/cert.pem
#status_backend = sqlite
#realdelete = no

